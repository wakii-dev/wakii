# Context pack — FI-458 SF-2: Remote-safe verify + --host passthrough

## Spec slice (chỉ phần SF-2)
- `story-verify` B4 (merged into dest): trước merge-base check, `git fetch <remote>
  <dest>` (origin hoặc remote của dest — detect từ branch config); offline/fetch fail
  → fallback dữ liệu local hiện có (giữ nguyên verdict logic, log ⚠ fetched-stale).
- `story-launch --host <host-id>`: forward `--host` vào `orca worktree create --host
  <host-id>` khi spawn worktree trên máy đích (SSH/peer). Máy đích cần repo + kit
  (kiểm tra qua orca CLI error surface — fail-loud có reason).
- `story-resume`: các check dest (B4-adjacent) dùng cùng fetch-first pattern.

## Touch map
- bin/story-verify (khối B4 — merge-base --is-ancestor HEAD dest)
- bin/story-launch (đã có --host từ SF-1 — pass vào orca worktree create)
- bin/story-resume (dest fetch pattern + verify flow)

## ACCEPTANCE (user-visible / verifiable)
1. Máy B push SF lên remote; máy A chạy story-verify (không pull) → B4 PASS nhờ fetch
2. Fetch fail (offline) → verdict dùng local data + log ⚠ stale — KHÔNG crash
3. story-launch --host <id> → worktree spawn trên host đích (orca CLI nhận --host)

## Boundary
KHÔNG: đổi logic gates khác (B1-B3, evidence/tdd) · đụng mobile/ · thêm daemon fetch
nền (fetch on-demand trong verify thôi)
