# Context pack — FI-458 SF-4: Convergence QA — 2-machine simulation harness

## Spec slice (chỉ phần SF-4)
**PRE: Phase 1 checkpoint — cut kit 2.12.0 + release TRƯỚC khi bắt đầu SF-4** (phased-release rule — cấm big-bang)
- Harness mô phỏng 2 máy KHÔNG cần máy thật: 2 clone của 1 fixture repo + 2 machine-id
  (`--machine-id machine-a` / `machine-b`) chạy story-launch song song trên cùng bracket
- Kịch bản bắt buộc: (1) race 2 máy cùng claim 1 SF → 1 thắng; (2) takeover: claim
  machine-a stale → machine-b chiếm; (3) commits-fresh override: agent-a còn commit
  → machine-b KHÔNG chiếm; (4) remote-merge: máy B merge SF vào dest push remote →
  máy A story-verify B4 PASS (fetch); (5) enabled=false regression zero-diff
- README distributed section: setup 2 máy thật (prereqs: repo remote, Linear, kit
  cài cả 2 máy, machine-id khác nhau), quy trình launch/claim/merge/release
  checkpoint theo phased-release rule

## Touch map
- tests/ harness mới (mẫu: race harness SF-1 mở rộng thành e2e suite)
- README.md (kit) — section Distributed (setup + quy trình)

## ACCEPTANCE (user-visible / verifiable)
1. Harness 5 kịch bản trên đều exit đúng kỳ vọng (green suite)
2. README đủ để người mới setup 2 máy chạy 1 bracket không cần hỏi
3. Kết quả harness = evidence cho release checkpoint (demo được)

## Boundary
KHÔNG: dùng máy thật thứ 2 trong CI (mô phỏng bằng clone+machine-id) · test các
kịch bản đòi network thật ngoài localhost git remote (file:// remote đủ)

## Decisions chốt sau critic
- Takeover = 2×TTL liên tiếp (N=2); git-guard worst-case: phát hiện MUỘN (máy cũ không push-guard) → chấp nhận (LAN + 1 maintainer) + check nhẹ STORY-COMPLETE (PR diff ≥2 head cùng SF → alert)
- Linear cách ly: bracket riêng + machine-id prefix `sim-` (không rác lên board thật)
- Zero-diff regression 5 mặt: launch / verify B4 / resume / watchdog passthrough / panel ẩn Machines
- README prereqs per-machine: orca app + workspace đăng ký + ~/.claude/.linear-key + push quyền wakii-dev + kit sync — thiếu Orca = mất launch
